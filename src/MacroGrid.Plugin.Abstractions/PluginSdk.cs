namespace MacroGrid.Plugin.Abstractions;

/// <summary>The Plugin SDK's own semver — matches docs/versioning.md's "Plugin SDK" row. Bump this alongside that row.</summary>
public static class PluginSdk
{
    public const string Version = "0.3.0";
}
