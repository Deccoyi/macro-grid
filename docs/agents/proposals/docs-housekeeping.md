# Documentation housekeeping

The `docs/` folder was sorted into `guides/`, `ui/`, `design/`, `plans/`, `releases/` and `agents/` (see `docs/README.md`). What is still open and needs the owner:

- `docs/agents/dependabot-2026-09.md`: the dependency round is finished, but its "Left" list still has open items. Delete the note when they are done.
- `docs/ui/color-bible-preview.html`: static preview page for `color-bible.md`; keep it only if the color guide is still maintained, otherwise fold the swatches into the guide.
- `docs/roadmap.md`: linked from `README.md`, `CONTRIBUTING.md` and the issue template, so it stays at the top of `docs/`. Its "Done" section is long; consider trimming it to what is next.
- `docs/plans/plugin-distribution-plan.md`: status "planned, not implemented". When the feature ships, move it to `docs/design/` (kept as a reference) or to `docs/done/`.
