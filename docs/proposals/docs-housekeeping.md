# Documentation housekeeping

Candidates to move to `docs/done/` (or delete) once the owner confirms they are finished. Nothing was moved in this branch because links point at several of them.

- `docs/dependabot-2026-09.md`: one-off review note of a finished dependency round. Move to `docs/done/`.
- `docs/color-bible-preview.html`: static preview page for `color-bible.md`; keep only if the color guide is still maintained, otherwise fold the swatches into the guide.
- `docs/roadmap.md`: linked from `README.md`, `CONTRIBUTING.md` and the issue template, so it is live. Its "Done" section is long; consider trimming it to what is next.
- `docs/plugin-distribution-plan.md`: status "planned, not implemented". Correct as is; move to `docs/done/` only after the feature ships.
- `docs/release-notes-v0.2.0-alpha.md`: matches a published release; could move next to future release notes in a `docs/releases/` folder.
